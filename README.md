# IPv6-Converter-And-Endianness-Utility

This C program converts four 32-bit integers into a full IPv6 address string while correcting for byte-order endianness, and includes a built-in automated testing framework to validate conversions. It demonstrates bit manipulation, formatted IPv6 output, and structured unit testing in C.
